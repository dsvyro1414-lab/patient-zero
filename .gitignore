# Python
ml-service/.venv/
__pycache__/
*.pyc

# trained artifacts (regenerate with `python src/train.py`)
ml-service/models/*.joblib
# except the served model — a fresh clone must score without a 378MB re-download
!ml-service/models/model.joblib

# real datasets (do not commit licensed/PII data)
ml-service/data/*.csv
# except the built Stanford frame: de-identified, date-shifted, public, 158KB.
# It is the only artifact of the 378MB download + label extraction.
!ml-service/data/stanford.csv

# env / secrets
.env
.env.local

# node / next
web/node_modules/
web/.next/
web/out/
web/.vercel/
*.tsbuildinfo

# os
.DS_Store
.gstack/

# personal health data — never commit
my_whoop_data_*/
my_whoop_data_*.zip
