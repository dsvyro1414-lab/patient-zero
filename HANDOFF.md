# Patient Zero — состояние проекта и следующие шаги

> Рабочая записка для продолжения в следующей сессии. Читать первой.
> Последнее обновление: 2026-07-11.

## 0. TL;DR

**Patient Zero** — веб-приложение: подключается к Whoop, учит личный физиологический
baseline и поднимает флаг, когда организм начинает бороться с инфекцией — за дни до
симптомов. Хакатон **ML Empowerment Build Challenge 2.0**, трек **AI for Health**.
Дедлайн: **2026-07-31, 09:45 GMT+3**. Соло. Сегодня 2026-07-11 → ~20 дней.

**Ядро валидировано на РЕАЛЬНЫХ досимптомных данных COVID (Stanford). Задеплоено и живо:**
- **Демо:** https://patient-zero-two.vercel.app (Vercel)
- **ML API:** https://patient-zero-ml-production.up.railway.app (Railway)
- **Код:** https://github.com/dsvyro1414-lab/patient-zero (public)

**Честный headline (реальные Stanford, один канал RHR):**
> Детектор ловит **8/27 = 30% эпизодов COVID ДО симптомов**, медианное опережение
> **5 дней**; в окне ±2 дня — 16/27 = 59%, медиана 1.5 дня; **~1 ложная тревога на
> 31 здоровый день**. Классификатор (вторичный) — AUC 0.55.

Осталось: Devpost-сабмишн + новая крупная фича «Симптомы + Анализы + AI-разбор» (спек в §12).
Деплой, полный редизайн UI, простой слой для пользователя и RU/EN — сделаны в этой сессии (§11).
Личный Whoop-кейс уже добавлен как честный пример (не как «доказательство»).

---

## 1. Что это и почему выигрывает

Два ML-слоя с чётким разделением ролей:

1. **Changepoint-монитор — ГЛАВНЫЙ детектор** (real-time, без меток). Личный robust
   baseline (median/MAD) + CUSUM по каждому сигналу, слитые во **взвешенное среднее**
   отклонений в σ (Health Deviation Index). Тревога — на устойчивом, подтверждённом
   несколькими каналами, многодневном сдвиге. Тот же метод (RHR-Diff), что в
   COVID-исследованиях Stanford. Питает утреннюю тревогу и replay-демо.
2. **Обученный классификатор — ВТОРИЧНЫЙ слой** (gradient boosting, subject-level CV,
   калибровка). На одноканальном RHR слабый (**AUC 0.55**) — честно показан как есть,
   а не спрятан. Ответ на «нельзя обучить на n=1»: модель учится на публичной когорте.

Фичи — **subject-normalized robust z-scores** → детектор device-agnostic. Baseline
**каузальный** (`shift(1)` до окна), CUSUM копит только вперёд — опережение достижимо
в реальном времени, не задним числом.

**Мапинг на рубрику** (Technical 30 / Creativity 20 / Impact 20 / UX 15 / Presentation 15):
- Technical: два реальных ML-слоя, метрики на реальной когорте, воспроизводимые
  эксперименты **включая честный негативный результат** (`ml-service/experiments/`).
- Creativity: интеграция с Whoop; changepoint как «то, для чего дневная гранулярность идеальна».
- Impact: 5-дневная фора на отдых/изоляцию.
- UX+Presentation: replay-таймлайн (money-shot) + Report с честной детекцией + дисклеймеры.

Цели: **AI for Health** (основная) + Best Use of ML + Most Innovative + Data-Driven Insights.

---

## 2. Архитектура и как приложение работает СЕЙЧАС

```
Whoop v2 API ──OAuth/CSV-экспорт──▶ ML-сервис (Python/FastAPI :8000)  ◀──/ml/*──  Web (Next.js :3000)
                                     ├ features.py   robust-z (каузально)          ├ Connect  (app/page.tsx)
                                     ├ changepoint.py CUSUM+HDI  ⭐ главный          ├ Today    статус на сегодня
                                     ├ train.py/model.joblib     вторичный          ├ Replay   таймлайн ⭐
                                     └ /evaluate /score-history /demo               └ Report   детекция + ROC
```

Рантайм-поток (что происходит при открытии приложения):
1. Веб (`web/next.config.mjs`) проксирует любой `/ml/*` на Python-сервис (`ML_SERVICE_URL`, дефолт `:8000`).
2. **Report** зовёт `GET /evaluate` → отдаёт `models/metrics.json` (+`roc.json`). Экран ведёт
   **досимптомной** чувствительностью детектора; ROC классификатора — вторичной панелью.
3. **Replay** зовёт `GET /demo` → сервис берёт реального COVID-субъекта из `stanford.csv`,
   прогоняет через `changepoint.analyze()` и возвращает по-дневный ряд (z-оценки, HDI,
   тревоги, «почему»). Таймлайн рисует маркеры ALARM и ONSET. **Это настоящая логика, не мокап.**
4. **Today** — статус «на сегодня» (тот же путь скоринга).
5. Один и тот же `features.robust_z` + `changepoint.analyze` используются и в обучении,
   и в реал-тайме, и в демо → нет train/serve skew.

Тренировка/скоринг **офлайн** (данные локальные). Модель `model.joblib` и `stanford.csv`
закоммичены → свежий клон воспроизводит цифры без скачивания 378 МБ.

---

## 3. Текущий статус

| Компонент | Статус |
|---|---|
| Реальные Stanford-данные → `data/stanford.csv` (116 субъектов, 27 COVID) | ✅ собрано, закоммичено |
| Детектор + честная оценка (`evaluate_detection`) + пороги настроены | ✅ 30% досимпт / 5д / ~1-на-31 |
| Классификатор (subject-level CV, калибровка) | ✅ AUC 0.55, вторичный |
| FastAPI `/evaluate` `/score-history` `/demo` | ✅ работает (:8000) |
| Report ведёт досимптомной детекцией | ✅ работает |
| Фронтенд: 4 экрана на живом API | ✅ работает (:3000) |
| Эксперименты (сигналы / warmup / spread / фьюжн) воспроизводимы | ✅ `experiments/` |
| Личный Whoop-кейс добавлен в README как честный пример | ✅ сделано |
| Всё в git + **публичный GitHub** | ✅ на `main` |
| Whoop v2 OAuth + backfill (код) | ✅ код готов, нужны креды приложения |
| **Деплой: web→Vercel, ML→Railway, живой линк** | ✅ работает end-to-end |
| **Devpost-сабмишн (видео, скриншоты, описание)** | ⬜ TODO |
| Доверительные интервалы в UI | ⬜ мелочь |
| Живой Whoop webhook | ⬜ стретч |

---

## 4. Данные (Stanford, РЕШЕНО)

Реально доступен только **Stanford COVID-19 Wearables** (Mishra 2020): публичный GCS-zip,
без заявок. Oura TemPredict мёртв (контракт), Scripps DETECT за DAA. Zip (378 МБ, разово):
`https://storage.googleapis.com/gbsc-gcp-project-ipop_public/COVID-19/COVID-19-Wearables.zip`
— поминутные HR/steps/sleep, **только RHR** (без HRV/дыхания/темп), меток внутри нет.

Метки (даты симптомов 32 COVID-субъектов) — в `data/mishra2020_supplementary.xlsx`
(лист SuppTable3). Даты PHI-сдвинуты, но сдвиг постоянен внутри субъекта → относительное
выравнивание «онсет vs физиология» сохранено (**проверено:** все 27 онсетов внутри диапазона).

`data/build_stanford.py` стримит zip, считает дневной resting-HR, берёт онсет, маскирует
не-COVID болезни → `data/stanford.csv`. `stanford.csv` уже собран и закоммичен — пересборка
нужна только если менять правила.

---

## 5. Как запустить

```bash
VENV=/Users/david/ML-Empowerment-Build-Challenge-2.0/ml-service/.venv/bin/python

# ML-сервис (stanford.csv и model.joblib уже в репо — пересборка/переобучение не нужны):
cd ml-service/src && $VENV -m uvicorn app:app --port 8000

# веб (терминал 2):
cd web && npm install && npm run dev            # http://localhost:3000

# переобучить (если менял детектор/данные) — пишет models/metrics.json:
$VENV ml-service/src/train.py --source stanford

# эксперименты (сигналы / warmup / spread-оценщик / фьюжн):
$VENV ml-service/experiments/run_experiments.py
```

Открыть http://localhost:3000 → Connect → Today → Replay → Report.

---

## 6. Карта репозитория (ключевое)

```
ml-service/src/
  features.py       robust-z (каузально, shift(1)); SPREAD_ESTIMATOR="lagged_mad";
                    MIN_BASELINE_DAYS=12 (теперь ИСТИННЫЙ прогрев)
  changepoint.py    baseline + CUSUM; HDI = ВЗВЕШЕННОЕ СРЕДНЕЕ σ; FUSED_ALARM=2.375;
                    evaluate_detection() → досимптомная + [-7,+2] + FA на scorable-днях
  train.py          subject-level CV + калибровка + detection-блок в metrics.json
  dataset.py        грузит stanford.csv (или синтетику); label из onset (окно [-3,+1])
  whoop_client.py   Whoop v2 OAuth + backfill + to_daily_frame()
  app.py            FastAPI: /evaluate /score-history /demo
ml-service/data/    build_stanford.py, stanford.csv, mishra2020_supplementary.xlsx
ml-service/experiments/  run_experiments.py + README (4 эксперимента)
ml-service/models/  model.joblib + metrics.json + roc.json
web/app/            page.tsx (Connect), (app)/{today,replay,report}/page.tsx
my_whoop_data_2026_07_10/   ЛИЧНЫЙ Whoop-экспорт (В .gitignore, НЕ коммитить)
```

---

## 7. Что сделано в этой сессии (2026-07-10) — важно

**Аудит (36 агентов + адверсариальная проверка + собственный пересчёт)** вскрыл цифры,
которые нельзя было подавать как есть, и три бага. Всё исправлено. Подробности —
в памяти проекта `patient-zero-audit-findings.md`. Кратко:

1. **Прогрев был удвоен** (24 дня вместо 12): MAD считалась от `(past - med)`, а он
   NaN, пока `med` не определится. Ранние эпизоды были недостижимы **по построению**. Починено.
2. **Порог слияния был СУММОЙ** по каналам → рос с их числом. Гейт, настроенный на
   одноканальном Stanford, на 5 каналах Whoop горел постоянно (1 ЛТ/5 дней). Теперь
   **взвешенное среднее** σ → один порог для 1 и 5 каналов. На Whoop: ЛТ 4→0.
3. **Баг выбора порога классификатора** (`_threshold_for_false_alarm` шёл сверху вниз →
   всегда max, ничего не срабатывало, `episode_sensitivity=0.0` текло в `/evaluate`). Починено.
4. **Честная переформулировка:** ведущая метрика теперь **досимптомная** (тревога строго
   ДО симптомов); знаменатель ЛТ очищен от дней прогрева.
5. **Оценщик разброса выбран абляцией** (`lagged_mad`) на Stanford с критерием,
   зафиксированным заранее. Эксперимент 4 воспроизводит.

**Личный Whoop прогнан (`my_whoop_data_2026_07_10/`, 54 дня, 5 сигналов, 4.0 ремешок).**
За день до первого симптома сигнал был явный: **RHR +2.7σ, HRV −4.7σ**, но детектор
**промолчал** — среднее по 5 каналам размыло два кричащих (HDI 1.56 < порог 2.38). ЛТ = 0.
**Порог под этот эпизод НЕ подкручивался** (это был бы тюнинг на n=1). Оговорка: в дневнике
та же неделя помечена аллергией — возможно, не инфекция. Добавлено в README как честный пример.

---

## 8. Следующие шаги (по приоритету)

### Деплой — ✅ СДЕЛАНО (2026-07-10)
- **web → Vercel**, **ML → Railway** (Docker: `python:3.12-slim`, обошли сломанный
  Railpack/mise). Живо и проверено end-to-end (Report и Replay рендерятся из Railway).
- **Как редеплоить:**
  - ML: `cd ml-service && railway up --ci --service patient-zero-ml`
  - web: `cd web && vercel --prod --yes --scope dsvyro1414-labs-projects`
  - env `ML_SERVICE_URL` уже задан в Vercel-проекте (Production). Vercel НЕ подключён к
    GitHub — деплой только руками через CLI.
- Whoop-экспорт прогонялся скриптом; `whoop_client.py` для live не подключён (стретч).

### Шаг B (осталось) — Devpost-сабмишн 🔴 (Presentation 15% + Design 15%)
- README уже честный ✅, живой линк есть ✅. Осталось: видео 60–90 сек, скриншоты
  4 экранов, описание (Problem / Solution / Key Features / Tech / Target Users).
- Сабмитить за ~2 дня до дедлайна (цель ~29 июля).

### Мелочи (дёшево, поднимают доверие)
- Доверительные интервалы в Report (досимпт. 30%, 95% ДИ [16%, 48%]).
- Дисклеймер «рабочая точка выбрана на этой же когорте» уже на Report ✅.

### Открытая исследовательская задача (не блокер сабмишна)
- **Правило слияния 5 каналов не валидировано** — одноканальный Stanford не отличает
  среднее от суммы/максимума. Личный Whoop показал, что среднее размывает 2 сильных мовера.
  Варианты: top-k среди каналов, max, или требовать сильный одиночный сигнал ИЛИ слабый общий.
  Проверять честно можно только на многоканальной когорте / нескольких своих эпизодах. **Не тюнить на n=1.**

### (стретч) Живой Whoop OAuth + webhook
- Отложено: хрупкий кусок для живой демо, мало очков. Код готов в `whoop_client.py`.

---

## 9. Честность / безопасность (держать в питче)
- **Не медицинский прибор.** Вывод — физиологический флаг, не диагноз.
- Все числа — на held-out публичных субъектах Stanford, один канал RHR. Рабочая точка
  детектора выбрана на той же когорте (раскрыто на Report).
- Ведущая цифра — **досимптомная** (тревога ДО симптомов). Окно ±2 дня показываем как контекст.
- **Многосигнальную силу НЕ заявлять** — на одноканальной когорте её не проверить.
- Baseline каузальный (без заглядывания вперёд) — опережение реально в real-time.

## 10. Заметки окружения
- **venv:** `/Users/david/ML-Empowerment-Build-Challenge-2.0/ml-service/.venv`. Python 3.13, Node 24.
- Нет Homebrew/libomp → xgboost не грузится, `train.py` падает на sklearn
  `HistGradientBoostingClassifier` (тоже настоящий gradient boosting). Ок.
- git: `main` = 5 коммитов, синхронно с публичным origin. Личный Whoop-экспорт в `.gitignore`.
- Данные и сборка Stanford — офлайн; интернет нужен только для деплоя, `npm install`, `git push`.

---

## 11. Сессия 2026-07-11 — редизайн UI, простой слой, RU/EN, фото WHOOP

Всё на ветке `claude/design-reproduction-46f056`, влито в `main` (fast-forward) и **задеплоено**.
Живой сайт https://patient-zero-two.vercel.app обновлён и проверен (RU+EN, реальные данные из Railway).

- **Полный редизайн фронтенда:** тёмная «премиум» тема с принудительным dark, компактный icon-rail
  сайдбар (`web/app/globals.css`, `web/components/Sidebar.tsx`).
- **Простой слой для конечного пользователя** (владелец просил «должно быть сильно проще, без жаргона»):
  - `/home` (Главная) — дружелюбный статус + Сегодня/Завтра риск простыми словами. Connect ведёт сюда.
  - `/forecast` (Прогноз) — крупная оценка риска %, прогноз по дням, недельный график. Без σ/HDI/ROC.
  - Технические экраны `/today` `/replay` `/report` оставлены как «детали / для жюри».
- **НЕ ОТКАТЫВАТЬ:** «уровень риска %» на home/forecast считается из HDI детектора
  (`web/lib/forecast.ts`, `riskPct = HDI/3.5`), а НЕ из классификатора (там 1–10% даже на тревоге —
  выглядело бы как ложный «10%» и противоречило статусу). Честная рамка «не диагноз» сохранена.
- **Реальное фото браслета WHOOP:** `web/public/whoop-band.png` (авто-подхват в `web/app/page.tsx`);
  `web/scripts/prep_band.py` убирает светлый фон+тень из исходника `web/scripts/whoop-band-source.jpeg`
  (2 прохода: заливка от краёв + удаление белизны внутри петли). SVG-фолбэк в `web/components/WhoopBand.tsx`.
- **Полная локализация RU/EN для судей:** словарь `web/lib/i18n.ts` (RU+EN, `fmt()` для чисел),
  серверный `web/lib/locale-server.ts` (cookie `lang` → `getT()`), клиентский
  `web/components/LocaleProvider.tsx` (`useT`), тумблер `web/components/LangToggle.tsx` в сайдбаре
  (внизу, над «выйти»). RU по умолчанию. Переключение = **полная перезагрузка** (`location.reload`),
  т.к. `router.refresh` давал десинк клиент/сервер. При добавлении новых строк — писать в ОБА языка.
- Деплой без изменений: `cd web && vercel --prod --yes` (линк `.vercel` скопирован из главного репо,
  добавлен в `.gitignore`). Vercel НЕ по git-триггеру — только руками через CLI.

## 12. Следующая крупная фича — «Симптомы + Анализы + AI-разбор» (замыкание петли) 🔴

Владелец подтвердил приоритет (2026-07-11): при подозрении на болезнь приложение помогает
**подробно описать симптомы**, **загрузить анализы**, и **AI даёт разбор** + подсказывает срочность и
к какому специалисту. Смысл — «замыкание петли»: сигнал носимого → подтверждающий тест → врач.

**⚠️ КРИТИЧЕСКАЯ РАМКА БЕЗОПАСНОСТИ (не переходить — иначе теряем главное преимущество, честность):**
- Всё — «информация для обсуждения с врачом», НИКОГДА не диагноз и не назначение.
- НЕ называть конкретные лекарства и конкретных врачей по имени. Только **специальность + срочность**.
- Анализы = чувствительные мед-данные (PHI): согласие, шифрование, **НЕ коммитить в публичный репо**,
  для демо хранить локально/в сессии.
- Сохранить все существующие дисклеймеры. Это сдвиг в сторону клинического триажа/SaMD — для реального
  продукта регуляторка; для хакатона делаем **прототип с явными оговорками**. Overclaim = минус у жюри.

**Три части (по приоритету эффект/риск):**
1. **Экран «Дальше» (триаж)** — 🟢 приоритет 1 (дёшево, безопасно, наглядно):
   - подробный опросник симптомов (температура, кашель, горло, насморк, потеря обоняния/вкуса,
     утомляемость, боли, ЖКТ и т.д. — «реально подробно», как просил владелец);
   - rule-based срочность от (риск-уровень + ответы): самонаблюдение → домашний тест →
     терапевт в 1–2 дня → **красные флаги** (одышка, боль в груди, спутанность) = срочно за помощью.
2. **Загрузка анализов + AI-разбор** — 🟡 приоритет 2 (флагман истории):
   - загрузка PDF/фото ИЛИ ручной ввод значений (CRP, лейкоциты и т.п.); ручной ввод надёжнее OCR для демо;
   - Claude API вытаскивает значения + вместе с физио-таймлайном генерит **«сводку для врача»**
     («RHR повышен 3 дня перед симптомами + CRP X — покажи на приёме»), НЕ диагноз;
   - модель — актуальный Claude (см. skill `claude-api` для id/цен/вызова).
3. **«К какому врачу»** — 🟢 встроить в триаж: специальность + срочность (терапевт/неотложка/телемед/ЭР),
   НЕ имена врачей; опционально generic «найти врача рядом».

**MVP-демка:** высокий риск → «Дальше» → подробные симптомы → «рассмотри тест, терапевт 1–2 дня» →
загрузил/ввёл CRP → Claude собрал сводку-таймлайн для врача. Всё с рамкой «не диагноз».

**Где строить (текущий стек):** новые экраны в `web/app/(app)/` (напр. `/next-steps` и/или `/labs`),
пункт(ы) в `web/components/Sidebar.tsx`, строки в `web/lib/i18n.ts` (RU+EN!), AI-вызовы — через route
handler в Next или в ML-сервисе. Симптомы/анализы — НОВЫЕ данные пользователя, не из Stanford-демо.
Приоритет 1 (триаж) сделать первым — он безопасный и наглядный; загрузку анализов — вторым.

## 13. Сессия 2026-07-11 (продолж.) — Приоритет 1 §12 «Дальше» (триаж) — ✅ СДЕЛАНО

Экран триажа из §12 построен, проверен и лежит в рабочем дереве (НЕ закоммичен/не задеплоен — жду владельца).

**Что сделано (все файлы в `web/`):**
- `app/(app)/next-steps/page.tsx` — серверная страница: берёт `getDemo()`, вычисляет уровень риска через
  `buildForecast` (тот же HDI-риск, что на home/forecast), сеет им клиентский опросник. Работает и без
  носимого (риск = low).
- `components/NextSteps.tsx` — клиентский опросник: живой результат, **всегда видимый красный баннер**
  «сразу за помощью», **липкая карточка результата**, ввод по категориям, красные флаги — последними и
  визуально выделены. Состояние только в React (эфемерно).
- `lib/triage.ts` — чистая **детерминированная** функция `triage(answers, risk)`. Правило: красные флаги
  (или темп. ≥40) → сразу `emergency` (ИЛИ-гейт первым); балл симптомов S → базовый тир; носимое может
  ТОЛЬКО ПОВЫСИТЬ срочность и капнуто на `gp_soon`; низкий сигнал НИКОГДА не понижает симптомный тир;
  этажи amber/high-fever. Тиры: monitor→home_care→gp_soon→same_day→emergency. Только специальность+срочность,
  БЕЗ лекарств/имён врачей.
- `lib/symptoms.ts` — каталог из 23 симптомов (8 красных флагов). **Каталог — единственный источник правды
  для красных флагов**: `triage.ts` берёт `RED_FLAG_KEYS` из него (новый флаг эскалирует автоматически).
- `lib/i18n.ts` — новый неймспейс `nextSteps` (RU+EN) + `nav.nextSteps`; кнопка `nextSteps.cta` на home/forecast.
- `components/Sidebar.tsx` — пункт `/next-steps`; CTA «Что делать дальше →» на home и forecast (ярче при риске).

**Безопасность (§12) соблюдена:** всё — «информация для врача, не диагноз»; нет названий лекарств/врачей;
данные опроса эфемерны (нет localStorage/fetch/логов/URL); все дисклеймеры (не-диагноз, приватность,
группы риска, «прототип», экстренный баннер) на экране.

**Провенанс/проверка:** спроектировано панелью из 4 линз (экстренные красные флаги · тиры первички ·
таксономия симптомов · аудит безопасности) → синтез-спека → реализация → адверсариальный ревью
(safety/engine/i18n) с верификацией находок → 5 исправлено (каталог-driven красные флаги; починен контракт
`preSymptomatic` при amber-этаже; удалён мёртвый код). `tsc --noEmit` + `next build` зелёные; живой QA в
RU+EN: досимптомный / emergency / same_day / clear-all.

**Осталось по §12:** Приоритет 2 — загрузка анализов + AI-разбор (Claude). ⚠️ Он ПЕРЕДАЁТ данные стороннему
API — НЕ переиспользовать копирайт «остаётся на устройстве»; нужен отдельный дисклеймер о передаче +
разовое согласие; AI-вызов через серверный route handler без хранения payload.

**Грабли:** не запускать `next build` пока крутится `next dev` на том же `.next` — портит dev-чанки
(`Cannot find module './NNN.js'`); лечится `rm -rf web/.next` + рестарт dev-сервера. Редеплой (когда решишь):
`cd web && vercel --prod --yes`.

## 14. Сессия 2026-07-12 — Приоритет 2 §12 «Анализы + AI-сводка» — ✅ СДЕЛАНО и ЗАКОММИЧЕНО

Замкнули петлю: сигнал носимого → подтверждающий тест → врач. Ветка `claude/ecstatic-jang-8d1611`:
Часть 1 (триаж) перенесена и закоммичена (`647e80b`); Часть 2 закоммичена (`16f8204`).

**Что сделано (всё в `web/`):**
- `app/(app)/labs/page.tsx` — серверная страница: собирает компактный физио-таймлайн из `getDemo`+
  `buildForecast`+`pickToday` (уровень риска, lead = onset−first_alarm, топ-3 отклонившихся сигнала) и
  передаёт клиенту. Работает и без носимого (physio=null).
- `components/LabsSummary.tsx` — клиент: **разовый гейт согласия на передачу данных** (отдельный от
  «остаётся на устройстве» триажа) → ручной ввод 6 маркеров (CRP, WBC, нейтрофилы, лимфоциты, PCT, СОЭ) +
  свободный текст + заметка о самочувствии → POST → карточка результата с копированием. Состояние только
  в React (эфемерно).
- `lib/labs.ts` — каталог маркеров + общий контракт запрос/ответ (клиент ⇄ route).
- **`app/api/labs-summary/route.ts`** — AI-вызов. Модель **`claude-opus-4-8`** (ZDR-совместима — верно для
  PHI; Fable 5 требует 30-дн. хранение — не подходит). `@anthropic-ai/sdk` 0.111, non-streaming
  `max_tokens: 1600`, без thinking (быстро/предсказуемо для route handler), цепочка типизированных исключений
  (`RateLimitError`→429, `APIError`→502), гвард `stop_reason==="refusal"`, проверка наличия
  `ANTHROPIC_API_KEY` → 503 `ai_unconfigured` (мягко, без падения). Системный промпт держит ВСЕ рамки: не
  диагноз, без лекарств/имён врачей, значения «спроси врача интерпретировать», приложение НЕ помечает
  high/low; структура «Что показывают цифры / Вопросы врачу / Возьми на приём» + дисклеймер; ответ на языке
  UI. Route НЕ хранит и НЕ логирует payload.
- `lib/i18n.ts` — неймспейс `labs` (RU+EN) + `nav.labs` + `nextSteps.labsCta`.
- `components/Sidebar.tsx` — пункт `/labs` (иконка-колба); CTA из триажа в `/labs` (замыкатель петли).

**Проверка:** `tsc --noEmit` + `next build` зелёные; живой QA в RU+EN — гейт согласия, форма всех 6 полей,
дисклеймеры, и путь ошибки route (POST → 503 `ai_unconfigured` → локализованный баннер). Happy-path Claude
вживую НЕ гоняли (в этом окружении нет ключа) — проверено структурно; физио-карточка вживую не показана,
т.к. исходников ML-сервиса нет в этом checkout.

**Хардненинг перед деплоем (коммит `09eb1f7`):** прогнали 4-полосный мультиагентный пред-деплой аудит
(с адверсариальной верификацией). Вердикт: код деплоить безопасно, но публичный платный эндпоинт даёт
реальный риск раскрутки счёта в момент, когда поставят ключ. Закрыли ДО деплоя, в `route.ts`: жёсткий
**лимит тела 24KB** (413, до parse — гасит ~20-25x раздувание контекста); `sanitizeLabs` дедуп по ключу
(≤6 записей); новый `sanitizePhysio` ограничивает атакуемый объект physio (конечные riskPct/leadDays,
whitelist riskLevel, ≤8 topSignals с обрезкой ключей — заодно закрыт вектор строковой инъекции);
**rate limit по IP + глобальный дневной** (429). Проверено вживую dummy-ключом: 413 на большом теле, 429 на
7-м запросе/мин, чистый 502 при доходе до SDK, без утечки стека. **Оговорка:** rate-лимитер in-memory/
per-instance (сбрасывается на cold start, не общий между инстансами) — demo-grade; для продакшена заменить на
Upstash Ratelimit / Vercel KV. Лимиты тела+полей — основной надёжный барьер по стоимости.

**ЗАДЕПЛОЕНО 2026-07-12** (коммит `09eb1f7`) — прод **https://patient-zero-two.vercel.app** (deploy
`dpl_G5774wXMpLoZVRLcvYDZ7CKCj9a2`). Редеплой: `cd web && vercel --prod --yes --scope dsvyro1414-labs-projects`.
Канарейка зелёная: `/`, `/labs`, `/next-steps` = 200; POST `/api/labs-summary` = 503 `ai_unconfigured` (мягко,
ключ не задан). В проде теперь и Часть 1 (триаж), и Часть 2 (анализы).

**Осталось владельцу:** включить AI-сводку — `cd web && vercel env add ANTHROPIC_API_KEY production` (ключ
ставит владелец — я не ввожу ключи) и редеплой, чтобы функция его подхватила (env-переменные Vercel
применяются к деплоям, созданным ПОСЛЕ установки). В Vercel Production уже есть `ML_SERVICE_URL`. Ничего из
введённых анализов НЕ коммитить (PHI). Для локали — `web/.env.local` (шаблон `web/.env.example`, в .gitignore).
